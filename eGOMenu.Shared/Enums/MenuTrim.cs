namespace eGOMenu.Shared.Enums;

public enum MenuTrim {
  /// <summary>No trimming is applied.</summary>
  NONE,

  /// <summary>Trim characters from the head (front) of the string if needed.</summary>
  HEAD,

  /// <summary>Trim characters from the tail (end) of the string if needed.</summary>
  TAIL
}